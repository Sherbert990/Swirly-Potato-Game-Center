{
  "name": "swirly-potato-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only front-end tests (Node's built-in test runner). Not shipped — the app is static HTML/JS + a Python backend.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
